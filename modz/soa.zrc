
# ----------------------------------------------------
# soa: "cflags"
# soa+ "cflags"
soa: ()
{
    while (($#argv)); do
        eval "export ${1}=()"
        shift;
    done
}
soa+ ()
{
    local key=$1
    shift
    while (($#argv)); do
        eval "$key+=\"${1}\""
        shift;
    done
}
