
# a lightweight and yet complete implementation of the MessagePack serialization format version 5. It also supports the Timestamp extension type.

pkg\ cuuid () {
    : wd "${PWD}/cuuid"
    __install__ ()
    {
        git clone https://github.com/d4tocchini/cuuid.git
    }
    # \ +cflags ()
    # {
    #     arr+ cflags -I"${wd}/src"
    # }
}
